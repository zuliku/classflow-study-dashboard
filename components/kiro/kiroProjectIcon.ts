import { Layers } from "lucide-react";

/**
 * Kiro Project 领域图标（App Chrome V2.1）：
 * Kiro Project = Conversation + Project Files + Instructions + Context 组成的
 * AI Context Workspace → Layers（而非普通文件夹 FolderKanban）。
 * KiroProjectPanel / KiroThreadRail 等全部项目入口共用此常量。
 */
export const KIRO_PROJECT_ICON = Layers;
